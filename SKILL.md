---
name: animated-sticker
description: 制作和优化 GIF 动态表情包：从角色参考、九宫格或动作序列裁帧，按需生成中间姿势，合成并验证循环动图。适用于微信自定义表情、动态贴纸和已有动图的连贯性修正；普通静态图片或长视频制作不使用。
---

# 动态表情包

交付真实多帧、可下载的循环 GIF。保持用户指定角色和画面，优先修复具体的动作断点，不以总帧数代替连贯性。

## 选择入口

- **已有九宫格/帧序列，只要求合成**：精确裁切与编码，不重新生成角色或画面。
- **从描述或角色参考开始**：先确定一个短动作循环与视觉锚点，使用当前环境可用的内置图像生成工具绘制动作帧。读取可用的 `imagegen` 技能，遵循其图片生成流程。
- **用户要求更顺滑或补图**：检查原 GIF 的实际动作断点，补相邻关键姿势之间的小幅变化。已有补帧授权后直接执行，无须反复确认。原先明确禁止重绘且尚未授权补图时，说明需要新姿势并询问是否接受。
- 图片未随引用对话传入时，先尝试可用的对话读取工具；仍没有原图就请用户上传，不能凭文字假装拿到了图。

本技能的脚本只负责确定性的裁切、拼帧和核验，不生成图像。生成新画面使用实际可用的图像工具；不要静默改用付费 CLI/API 或另一服务。

## 动作与裁切

1. 看原图，识别动作顺序、文字、边框和格子实际边界。默认行优先仅适用于画面顺序与剧情一致时。
2. 对九宫格或 sprite sheet，检查**实际**行列数及格子是否完整。生成结果可能少列、末列被截断或边界不等宽；不要直接相信提示词要求的布局。用明确的裁切框移除分隔线，保持共同画幅与比例，避免逐格紧贴主体裁切造成抖动。
3. 用锚点追踪头部中心、眼线、爪子、被子边缘等动作对象；背景、相机、角色尺度尽量固定。字幕若跳动，可在用户允许范围内采用固定字幕，不擅自删除用户要求保留的文字。
4. 用户要求补帧时，读取 [动作补帧指南](references/motion.md)。先修复最大跳变，再处理首尾。新生成帧可替换不合适的旧帧，不为凑数量保留坏帧。

## 合成与检查

使用 Pillow 与 [合成脚本及清单格式](references/build.md)。在当前任务的 `work/` 写中间文件，在用户指定输出目录交付。运行脚本前确认 Python 与 Pillow 可用；Windows 的系统占位 `python.exe` 可能不可用，应查找已安装运行时，不能硬编码创建本技能时的机器路径。

- 帧时长使用 10 ms 的整数倍。中间姿势可先试 50–90 ms，关键表情停顿 200–500 ms；按实际动作调整。补图时重新分配时间，避免单纯拉长动画。
- 240×240 像素、统一 128/256 色调色板可作为小表情起点，具体服从用户需求。不要把这些建议写成微信官方限制。只有声称当前平台限制时才查证官方资料。
- 文件过大时先减少调色板颜色、无意义停留/重复帧，再考虑缩小尺寸；不要破坏关键动作去追求任意大小。最终报告实际大小，不能保证所有客户端均可保存。
- 渐变只是画面混合，会产生双影，**不等于新动作姿势**。只有用户接受柔和转场时才使用；不要为“更流畅”默认增加渐变。
- 重新打开最终 GIF，记录实际帧数、不同解码画面数、各帧时长、总时长、尺寸、字节数和 `loop=0`。GIF 编码器可能合并相同连续帧，以解码结果为准。
- 查看从成品解码的帧联系表，重点检查片段交接、顺序和最后一帧到第一帧。可用播放器时看实际循环；静态联系表与数值差异不能证明主观流畅，也不能冒充微信实测。
- 相邻差异分数只定位候选跳变，不是质量评分。核验低分是否只是重复画面，高分是否是合理动作。

## 迭代与交付

默认先制作一个完整版本，再对明确发现的主要断点作一轮针对性修正。若仍有明显身份漂移或大幅跳动，如实描述剩余问题，不无限生成、不为了达到某个帧数反复消耗资源。继续优化以用户授权、指定预算和可用工具为边界。

用绝对路径提供最终 GIF 下载与动图预览，简短说明本次变化及已核验的数据。补图版本保存生成提示词与采用的原始素材；按图像工具/技能要求提供提示词记录。区分：文件已生成、技术属性已验证、视觉已检查、平台实际发送已测试。只报告完成的检查。

用户说“定稿/还不错”后保留当前版本；之后的编辑另存新版本。不要将某只狗、某个故事、65 帧或本机路径作为通用要求。
