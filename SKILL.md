---
name: animated-sticker
description: 制作和优化 GIF 动态表情包：从角色参考、九宫格或动作序列裁帧，按需生成中间姿势，处理固定背景、动作缓动和循环衔接，合成并验证动图。适用于微信自定义表情、动态贴纸和已有动图的连贯性修正；普通静态图片或长视频制作不使用。
---

# 动态表情包

交付可下载的真实多帧 GIF。先保证动作和画面，再调整帧数、速度和体积；增加帧数不能替代动作设计。

## 先确定基准和问题

- 用户选定或认可的版本就是后续基准，保留原文件，修改另存新版。回到某一版时，从那个实际文件继续，不沿用后来被否定的嘴型、手势或变形。
- 分清本轮范围：调速、去背景、补帧、改动作。只调速时保留画面顺序；只分离背景时保留人物原像素和时序；补帧时先决定总时长，避免顺带拉长动作。
- 有动作参考视频时先查看原视频及关键时刻，记录手、头、表情、身体的配合与先后节奏。只移动手或整张角色图，不能冒充复刻完整表演。未能读取视频时明确说明依据有限。
- 缺少原图时先尝试可用的对话读取能力，仍无法取得再请用户补文件，不凭文字假装见过素材。

## 选择处理路线

| 任务/现象 | 首选处理 |
| --- | --- |
| 已有九宫格，只要合成 | 测量实际格子边界，裁切、去间隔、统一构图，不重新设计人物 |
| 新角色或缺少关键姿势 | 用可用图像工具生成有明确起终点的小段动作，依工具要求读取相应技能 |
| 少量姿势差异小、没有显著遮挡变化 | 可尝试运动对齐补间与节奏调整；读取 [动作指南](references/motion.md) |
| 大幅转头、遮挡/露出、手形或嘴型结构变化 | 优先补真实中间姿势；光流不能可靠补出不可见内容 |
| 背景闪烁、颜色漂移 | 分离人物与固定底图；读取 [背景和验收](references/background-and-qa.md) |
| 折返生硬、感觉“鬼畜” | 分别检查姿势间距、转向速度和首尾接缝，不能只改播放速度 |

用户已要求顺滑补图时，在授权范围直接处理；若明确禁止重绘，优先保留原画的办法，确实必须新增姿势时才解释限制。禁止把复制帧、全图淡化或几何变形称为新绘制姿势。已有像素的裁切、遮罩、运动对齐和编码可用脚本；新画面仍用图像生成工具，不擅自切换付费服务。

## 制作和验收

1. 裁帧按实测边界和动作顺序进行，不能把裁切残缺当成正常姿势。检查人物尺度、头部中心、手部位置、字幕锚点和镜头稳定性。
2. 动作先修关键断点，再处理转向和末帧→首帧。小幅往返可用连续缓动，非对称表演不能默认倒放。具体适用条件与命令见 [动作指南](references/motion.md)。
3. 背景需静止时使用同一底图和统一调色板；补间后恢复确认始终可见的背景区域。人物边缘及新露出区域仍要目检，不能只查四角。
4. 使用 [合成工具](references/build.md) 或可选的 `scripts/smooth_loop.py`。在工作区放中间产物，查找实际可用 Python，不硬编码某台机器的运行时路径。
5. 先确定每轮时长再分配帧。GIF 时长使用 10 ms 的倍数，工具最低 20 ms；不要为了保留帧数悄悄写成 10 ms。若目标帧数与速度冲突，说明取舍。240×240、128/256 色可作体积起点，不是微信官方上限。
6. 重新解码成品，记录实际帧数、不同画面数、每帧时长、总时长、尺寸、字节数、循环属性。查看动作端点、中间过渡、转向及跨循环的联系表；有播放能力时检查连续数轮。

文件检查、视觉检查、用户认可和微信发送实测分别报告。像素差值只定位候选问题，不证明自然流畅。详见 [背景和验收](references/background-and-qa.md)。

## 迭代和交付

默认交付一个推荐版本，用户要求时再提供快慢对照，确保比较的是同一组动作。提供绝对路径下载和动图预览，说明改动与实际验证结果。文件过大时优先优化调色板、冗余帧或尺寸，不能破坏关键动作或伪报平台兼容。

每次围绕明确问题做一轮修正；仍有变形就记录具体部位和限制，不自动无限重生成。已认可的动作及时保存为基准。参考 [经过用户反馈的案例](references/case-studies.md)，但不要把特定角色、60/65 帧或某个循环时长写成所有任务的模板。
